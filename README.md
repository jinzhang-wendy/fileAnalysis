# 📄 AI 智能文档分析工具

> 一个基于人工智能的文档分析工具，让你的文档"开口说话"

---

## 🎯 这个工具能做什么？

想象一下，你有一份 100 页的产品说明书，你想知道：
- "这个产品的保修期是多久？"
- "退货流程是怎样的？"
- "主要功能有哪些？"

**传统方式**：你需要一页一页翻阅、搜索关键词，可能要花上十几分钟。

**使用本工具**：
1. 上传文档
2. 直接提问
3. 秒速得到答案（还会告诉你答案在文档的哪个位置）

---

## ✨ 核心功能

### 1. 📤 智能文档解析
支持多种常见文档格式，自动提取文字内容：

| 格式 | 说明 |
|------|------|
| `.txt` | 纯文本文件 |
| `.md` | Markdown 文档 |
| `.pdf` | PDF 文档 |
| `.docx` | Word 文档 |
| `.xlsx` / `.xls` | Excel 表格 |
| `.csv` | CSV 数据文件 |

### 2. 🔍 四种分析模式

#### 模式一：智能识别（推荐）
让 AI 自动判断文档类型，选择最合适的分析方式：
- 流程类文档 → 生成流程图
- 报告论文 → 生成摘要和要点
- 合同规范 → 提取关键条款和实体

#### 模式二：流程图生成
将文档中的流程描述转换成可视化流程图，一目了然。

#### 模式三：内容摘要
自动生成文档的：
- 标题
- 核心摘要（200字以内）
- 关键要点列表
- 关键词标签

#### 模式四：结构分析
分析文档的层级结构：
- 章节划分
- 各章节摘要
- 关键实体提取（人物、组织、日期、金额等）

### 3. 💬 智能问答（核心功能）

这是本工具最强大的功能。你可以像和人对话一样，向文档提问：

```
用户：这个产品的保修期是多久？
AI：根据文档第 3 页第 2 段，本产品保修期为 2 年，自购买之日起计算。

用户：那如果坏了怎么办？
AI：根据之前提到的保修期和文档第 5 页的售后流程，您需要：
    1. 联系客服热线 400-xxx-xxxx
    2. 提供购买凭证
    3. 寄回产品进行检测...
```

#### 什么是"长短记忆联动"？

我们模拟了人类的记忆方式：

| 记忆类型 | 类比人类 | 在工具中 |
|---------|---------|---------|
| **长期记忆** | 你学过的知识、看过的书 | 文档内容（通过 RAG 技术检索） |
| **短期记忆** | 刚才聊天的内容 | 最近几轮对话记录 |

当你追问"那如果坏了怎么办"时，工具会：
1. **长期记忆**：从文档中检索"售后"、"维修"相关内容
2. **短期记忆**：结合之前提到的"保修期"，理解你在问保修期内的维修流程
3. **综合回答**：给出完整的解决方案

### 4. 🛡️ 稳健的错误处理

- **网络超时**：自动重试，最多等待 30 秒
- **请求失败**：智能重试，间隔递增（1秒→2秒→4秒）
- **随时取消**：点击"取消"按钮中断正在进行的操作
- **友好提示**：错误发生时告诉你具体原因和解决方法

---

## 🚀 快速开始

### 第一步：安装 Node.js

本工具基于 Node.js 运行，需要先安装它。

**Windows 用户：**
1. 访问 [https://nodejs.org/](https://nodejs.org/)
2. 下载 LTS（长期支持版）安装包
3. 双击安装，一路"下一步"即可

**Mac 用户：**
```bash
# 如果已安装 Homebrew，直接运行：
brew install node

# 没有安装 Homebrew？访问 https://nodejs.org/ 下载安装包
```

**验证安装：**
打开终端/命令行，输入：
```bash
node -v
npm -v
```
如果显示版本号（如 v20.x.x），说明安装成功。

### 第二步：下载项目

```bash
# 方式一：使用 Git（推荐）
git clone https://github.com/your-username/ai-doc-analyzer.git
cd ai-doc-analyzer

# 方式二：直接下载 ZIP
# 1. 点击项目页面右上角的 "Code" → "Download ZIP"
# 2. 解压后进入项目文件夹
```

### 第三步：安装依赖

```bash
npm install
```

这一步会下载项目需要的所有第三方库，首次安装可能需要几分钟。

### 第四步：配置 API Key

本工具使用 DeepSeek 的 AI 服务，需要获取 API Key（类似一把"钥匙"）。

#### 如何获取 DeepSeek API Key：

1. 访问 [https://platform.deepseek.com/](https://platform.deepseek.com/)
2. 注册/登录账号
3. 进入"API Keys"页面
4. 点击"创建 API Key"
5. 复制生成的 Key（以 `sk-` 开头的一串字符）

#### 配置项目：

在项目根目录创建 `.env.local` 文件：

```bash
# 在项目文件夹中创建配置文件
# Windows（命令行）：
echo VITE_DEEPSEEK_API_KEY=sk-你的密钥 > .env.local

# Mac/Linux：
echo "VITE_DEEPSEEK_API_KEY=sk-你的密钥" > .env.local
```

或者用任何文本编辑器创建 `.env.local` 文件，内容如下：
```
VITE_DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
```

> ⚠️ **重要提示**：
> - 不要把 API Key 分享给他人
> - 不要把 `.env.local` 文件上传到公开的代码仓库
> - DeepSeek 新用户通常有免费额度，足够日常使用

### 第五步：启动项目

```bash
npm run dev
```

看到类似以下输出，说明启动成功：
```
VITE v7.x.x  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

### 第六步：开始使用

1. 打开浏览器，访问 `http://localhost:5173`
2. 点击"选择文件"上传你的文档
3. 等待文档解析完成
4. 点击"建立索引"（首次问答前需要）
5. 开始提问或选择分析模式

---

## 📖 使用教程

### 场景一：快速了解一份新文档

**你的情况**：老板给了你一份 50 页的行业报告，让你下午开会前了解主要内容。

**操作步骤**：
1. 上传 PDF 文档
2. 选择"智能识别"模式
3. 点击"分析文档"
4. 几秒后，你会看到：
   - 文档类型：行业报告
   - 核心摘要（200字概括全篇）
   - 5-8 个关键要点
   - 关键词标签

**结果**：5 分钟搞定，开会不心慌。

### 场景二：查找文档中的特定信息

**你的情况**：你在写一篇论文，需要引用之前看过的一篇文献中的实验参数，但忘记了具体数值。

**操作步骤**：
1. 上传那篇 PDF 文献
2. 点击"建立索引"
3. 提问："实验中使用的参数值是多少？"
4. AI 会给出答案，并标注来源段落

**结果**：精准找到，不再大海捞针。

### 场景三：理解复杂的流程文档

**你的情况**：公司的新员工入职流程文档，文字描述又长又绕。

**操作步骤**：
1. 上传 Word 文档
2. 选择"流程图"模式
3. 点击"分析文档"
4. 自动生成可视化流程图

**结果**：一图胜千言，照着流程走就行。

### 场景四：追问和深入探讨

**你的情况**：你在阅读一份产品手册，想深入了解某个功能。

**对话示例**：
```
你：这个产品支持哪些视频格式？
AI：根据文档第 8 页，支持 MP4、AVI、MOV、MKV 四种格式。

你：MP4 有什么限制吗？
AI：根据文档第 8 页的补充说明，MP4 格式有以下限制：
    - 最大分辨率：4K (3840×2160)
    - 最大文件大小：2GB
    - 推荐编码：H.264

你：那如果我想播放更大的文件怎么办？
AI：根据之前提到的限制，建议您：
    1. 将大文件分割成多个小于 2GB 的片段
    2. 或使用文档第 9 页提到的"网络播放"功能
       直接从 NAS 或云存储播放...
```

---

## ❓ 常见问题（FAQ）

### Q1: 我不懂编程，能用这个工具吗？

**A:** 完全可以！只要你按照上面的"快速开始"步骤操作，都是简单的复制粘贴和点击。使用时更是完全图形化界面，零编程门槛。

### Q2: 为什么是 DeepSeek？能用 ChatGPT 吗？

**A:** DeepSeek 是国产 AI 服务，价格实惠、中文效果好、国内访问稳定。如果你想使用其他 AI 服务（如 ChatGPT、Claude），可以修改代码中的 API 地址和模型名称，但需要一定的编程知识。

### Q3: 我的文档包含敏感信息，安全吗？

**A:**
- 文档内容只会在你的电脑上处理
- 发送给 AI 的只是文字内容（不含文件名、元数据等）
- 不会保存到任何第三方服务器
- 建议不要上传涉及国家机密、核心商业机密的文档

### Q4: API 调用要花钱吗？

**A:**
- DeepSeek 新用户有免费额度
- 正常使用情况下，分析一份文档 + 多轮问答，费用通常不到 1 分钱
- 如果担心费用，可以在 DeepSeek 后台设置使用限额

### Q5: 为什么有时候回答不准确？

**A:** AI 不是万能的，以下情况可能导致回答不准确：
- 文档内容本身模糊或有歧义
- 问题太笼统，无法精准匹配
- 文档太短，信息不足

**建议**：
- 尽量问具体的问题
- 可以追问获得更准确的答案
- 对重要信息，最好对照原文核实

### Q6: 支持图片中的文字识别（OCR）吗？

**A:** 当前版本不支持。如果你的 PDF 是扫描件（图片形式），需要先用 OCR 工具转换成可搜索的 PDF 或文本文件。

### Q7: 能处理多大的文档？

**A:**
- 前端限制：单文件最大 10MB
- 实际处理能力取决于文档类型和内容密度
- 一般 100 页以内的文档都能流畅处理

---

## 🔧 技术原理（给好奇的你）

### 什么是 RAG？

RAG（Retrieval-Augmented Generation，检索增强生成）是一种让 AI "记住"大量信息的技术。

**通俗理解**：

想象你要参加一场开卷考试：
- **传统 AI**：只能靠"记忆"，考场上不能翻书，可能记不全
- **RAG**：允许带一本参考书，遇到问题先翻书找答案

我们的工具就是这样：
1. 你的文档就是那本"参考书"
2. 提问时，先从文档中检索相关段落
3. 把相关段落和问题一起发给 AI
4. AI 基于文档内容回答，答案更准确

### 项目架构

```
ai-doc-analyzer/
├── src/
│   ├── components/          # 界面组件
│   │   ├── UploadParser/    # 文件上传和解析
│   │   ├── DocAnalysis/     # 文档分析和问答
│   │   └── FlowChart/       # 流程图展示
│   │
│   ├── services/            # 核心服务
│   │   ├── ragService.ts    # RAG 智能问答引擎
│   │   ├── apiClient.ts     # API 请求处理
│   │   └── logger.ts        # 日志记录
│   │
│   └── utils/               # 工具函数
│       ├── documentParser.ts # 文档解析器
│       ├── validators.ts     # 数据验证
│       └── errors.ts         # 错误处理
│
├── package.json             # 项目配置
└── README.md               # 说明文档
```

### 技术栈

| 技术 | 用途 |
|------|------|
| React | 用户界面框架 |
| TypeScript | 类型安全的 JavaScript |
| Tailwind CSS | 样式框架 |
| React Flow | 流程图渲染 |
| Mammoth | Word 文档解析 |
| PDF.js | PDF 文档解析 |
| SheetJS | Excel 文件解析 |
| DeepSeek API | AI 智能服务 |

---

## 🛠️ 高级配置

### 自定义超时时间

默认 API 请求超时为 30 秒，可以在 `src/services/apiClient.ts` 中修改：

```typescript
const DEFAULT_CONFIG: ApiClientConfig = {
  defaultTimeout: 30000,  // 修改这里，单位毫秒
  maxRetries: 3,
  baseRetryDelay: 1000,
};
```

### 调整记忆容量

默认保留最近 3 轮对话（6 条消息），可以在 `src/services/ragService.ts` 中修改：

```typescript
const SHORT_TERM_MEMORY_LIMIT = 6;  // 修改这里，建议 4-10 之间
```

### 更换 AI 模型

如果你想使用其他兼容 OpenAI 格式的 API：

1. 修改 `src/services/ragService.ts` 中的 `DEEPSEEK_API_URL`
2. 修改模型名称（如 `deepseek-chat` → `gpt-4`）
3. 更新 `.env.local` 中的 API Key

---

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

1. Fork 本项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建 Pull Request

---

## 📄 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

---

## 🙏 致谢

- [DeepSeek](https://www.deepseek.com/) - 提供强大的 AI 能力
- [React Flow](https://reactflow.dev/) - 优雅的流程图组件
- [Tailwind CSS](https://tailwindcss.com/) - 实用的样式框架

---

## 📮 联系方式

如有问题或建议，欢迎：
- 提交 [Issue](https://github.com/your-username/ai-doc-analyzer/issues)
- 发送邮件至 your-email@example.com

---

<p align="center">
  <b>如果这个项目对你有帮助，请给一个 ⭐️ Star 支持一下！</b>
</p>
